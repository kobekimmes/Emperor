package kobekimmes.com.emperor;

import java.util.List;
import java.util.Set;

class Random extends Bot {
    @Override
    void produceMove(Game game) {

//        game.produceLegalMovesForGameState(true);
//        Set<Piece> keys = game.legalMoves.keySet();
//
//        // Convert the Set to an array
//        Piece[] keysArray = keys.toArray(new Piece[0]);
//
//        // Generate a random index to select a random key
//        Random random = new Random();
//        int randomIndexForKey = (int) (Math.random() * keysArray.length);
//
//        // Get the random key using the random index
//        Piece randomKey = keysArray[randomIndexForKey];
//
//        // Retrieve the ArrayList associated with the random key
//        List<Position> randomArrayList = game.legalMoves.get(randomKey);
//
//        // Generate another random index to select a random element from the ArrayList
//        int randomIndexForElement = (int) (randomArrayList.size() * Math.random());
//
//        // Get the random element from the ArrayList using the second random index
//        Position randomPos = randomArrayList.get(randomIndexForElement);
//
//        nextPiece = randomKey;
//        nextPosition = randomPos;


    }
}